{
  "name": "miniprogram-ui-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI automation tests for miniprogram",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0"
  }
}
