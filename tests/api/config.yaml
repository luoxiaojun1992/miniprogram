server:
  port: 8080
  mode: debug

database:
  host: mysql
  port: 3306
  user: root
  password: secret
  name: miniapp

jwt:
  secret: test-jwt-secret-for-api-tests
  expiry: 7200

log:
  level: debug

wechat:
  app_id: test-wechat-app-id
  app_secret: test-wechat-app-secret

upload:
  dir: storage/uploads
  base_url: http://localhost:8080/static

debug:
  enable_test_token: true
